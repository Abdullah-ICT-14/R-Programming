setwd("J:/R-programming")
# Addition
4+5
# Subtraction
4-5
# Multiplication
3*8
# Division
4/5
# Power
5^2
# Equation
(7+3)*9-2
# Infity when anything is divided by zero
7/0
# Inf(Infity) when anything is divided by zero
7/0
x+y
x<-5
y<-7
x+y
x-y
x/y
x*y
# storing the results in new variables
add=x+y
add
str<- "string variables"
str
xx=TRUE
xx
yy <- FALSE
yy
# Find the class of the variables
# Class represents the abtract type of the object.
class(x)
class(str)
class(xx)
typeof(x)
typeof(str)
typeof(xx)  #
xx
yy
xx+yy
xx/yy
xx*yy
# To find all of the variables we already use we can using ls()
ls()
# For removing the variables rm() functions are used.
rm(x)
ls()
# remove multiple variables
rm(xx,yy)
ls()
# TO remove all the variables
rm(list = ls())
ls()
x<-5
y<-7
x+y
x-y
x/y
x*y
# storing the results in new variables
add=x+y
add
# String variables
str<- "string variables"
str
xx=TRUE
xx
yy <- FALSE
yy
class(x) # numeric class
class(str) # Character class
class(xx) # logical class
typeof(x)  # double type
typeof(str) # character type
typeof(xx)  # logical
# We can perform mathematical operations in logical variables.
# TRUE=1,FALSE=0
# Here xx=TRUE,yy=FALSE
xx
yy
xx+yy
xx-yy
xx/yy
xx*yy
# To find all of the variables we already use we can using ls()
ls()
