# Firtst we need to install the package from the packages section

# To activate the package we need to write the name of the package within the library() or require().
require(vcd)

# Deactivate the package
detach(package:vcd)